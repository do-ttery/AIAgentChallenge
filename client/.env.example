# 화면 환경변수 — 이 파일을 client/.env 로 복사해 값을 채운다.
# Vite는 VITE_ 접두사가 붙은 값만 번들에 넣는다 — 여기엔 공개해도 되는 값만 둔다.

# Web Push 구독에 쓰는 공개키.
# server/.env의 VAPID_PUBLIC과 반드시 같은 값이어야 한다 (짝이 안 맞으면 발송 실패).
VITE_VAPID_PUBLIC=
