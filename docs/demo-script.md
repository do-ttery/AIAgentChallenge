# 빨래집사 데모 영상 대본 · PPT 초안

> 마감: 2026-07-29(수) 22:00 · 총 길이 **~4:35** (5분 미만) · 음성 나레이션 · PR: `feature/demo-video → dev`
> 구조: **PPT(설명) → 화면녹화(시연) → PPT(기술·문제해결·Agent)** 3막 샌드위치

디자인 톤(PPT): Pretendard · Primary `#6366F1` · 마스코트(🧺) 표지·클로징에만 · 고객 관점 문구는 비난 금지.
상태 색 참고 — 대기 `#94A3B8` / 세탁중 `#6366F1` / 탈수 `#2E9CC5` / 완료 `#4CAF50` / 방치 `#F08C00`.

---

## 1막 · PPT — 서비스 설명 (0:00–0:45)

### 슬라이드 1 (문제) · 0:00–0:14
**제목:** 무인빨래방의 빈틈
- 사장님은 매장에 늘 계실 수 없다
- 끝난 세탁물이 방치되면 다음 손님이 기계를 못 쓴다
- → 회전율은 떨어지고, 사장님 개입은 늘어난다

**나레이션:** "무인빨래방은 사장님이 매장에 늘 계실 수 없습니다. 세탁이 끝나도 찾아가지 않는 빨래가 매장 회전을 막습니다. 빨래집사가 대신 챙깁니다."

### 슬라이드 2 (서비스 + Observe·Think·Act) · 0:14–0:45
**제목:** 빨래집사 — 전력으로 읽는 세탁 상태
**한 줄:** 전력 데이터로 시작·종료·방치까지 스스로 감지하는 AI 에이전트
- **Observe** — 스마트플러그 전력 · 문열림 센서
- **Think** — 6상태 상태머신으로 상황 판단
- **Act** — 고객 알림 · 사장님 알림 · 매장 안내
*(하단 각주: MVP 지원 범위 = 20kg · 표준 코스)*

**나레이션:** "빨래집사는 전력 데이터로 세탁 시작과 종료, 방치 여부까지 스스로 감지하는 AI 에이전트입니다. 감지하고, 판단하고, 행동한다 — Observe, Think, Act 세 단계로 움직입니다. 지금부터 실제 매장 세탁기로 이 흐름을 직접 보여드리겠습니다."

*(전환: "그럼 실제로 어떻게 도는지 보시죠" → 화면녹화)*

---

## 2막 · 화면녹화 — 동작 시연 (0:45–2:55)

### ① 실매장 라이브 · 세탁기 4번 (0:45–1:35)
**촬영:** 실 전력 라이브(플러그가 꽂힌 세탁기 4). 시작 순간·SPIN 전환 순간만 편집 발췌.
**사전 세팅(현장):**
- `npm run dev` (client 5173 / server 3000)
- `python power_poll.py --host <P110M IP> --machine-id <세탁기4 uuid> --interval 5 --server http://localhost:3000`
- `python door_poll.py --host <H100 IP> --nickname "<T110 별명>" --machine-id <세탁기4 uuid> --interval 5 --server http://localhost:3000`
- 브라우저: `localhost:5173/m/<세탁기4 uuid>` + `localhost:5173/owner`

**나레이션:**
- (시작) "이 세탁기는 4번, 20킬로그램 모델이고 표준세탁으로 시작합니다. 빨래집사가 지원하는 건 이 20킬로그램 기종과 표준 코스뿐입니다 — 다른 용량·코스는 아직 검증 전이라 MVP 범위 밖입니다."
- (RUNNING 전환) "아무도 누르지 않았는데 화면이 방금 '세탁 중'으로 바뀌었습니다. 전력이 100와트를 넘은 걸 서버가 감지한 결과입니다."
- (SPIN, 탈수음과 겹쳐) "지금 탈수 소리 들리시죠. 전력이 500와트를 넘는 순간 화면도 '탈수'로 바뀝니다."

### ② 종료 판정 · 로컬 사전녹화(세탁기 3) (1:35–1:55)
`node server/scripts/simulate.mjs --scenario normal --machine-name "세탁기 3"` — 60초 hold 후 DONE 순간만.
**나레이션:** "전력이 20와트 이하로 떨어진 뒤 60초 유지되면 종료로 판정합니다. 이 세탁기는 히터가 없어 세탁 중에도 전력이 잠깐씩 떨어지는데, 실측한 최대 8초와 확실히 구분하려고 60초로 잡았습니다."

### ③ 방치 자동 대응 · 로컬 사전녹화(세탁기 3) (1:55–2:25)
`TEST_ABANDONED_AFTER_MIN=1 TEST_COLLECT_REMINDER_INTERVAL_MIN=1` + `--scenario no-qr --machine-name "세탁기 3"`. 고객 안내 모드 + 대시보드 "사장님 손이 필요해요" 동시.
**나레이션:** "이 손님은 알림을 신청하지 않았습니다. 끝나고 15분간 문이 안 열리면, 집사가 연락할 방법이 없다는 걸 스스로 압니다. 고객 화면은 다음 손님 안내 모드로 바뀌고, 대시보드 맨 위에 '사장님 손이 필요해요' 카드가 뜹니다. 진짜 손이 필요한 순간에만 알리는 게 이 서비스의 핵심입니다. 시연은 15분을 1분으로 줄였을 뿐, 실제 기준은 15분입니다."

### ④ 실 푸시 + 딥링크 · 로컬 크롬(세탁기 3) (2:25–2:55)
크롬에서 구독 → 완료 도달 → 실제 배너 → 클릭 → 해당 세탁기 화면 이동.
**나레이션:** "알림 신청은 버튼 하나, 앱 설치도 연락처도 없습니다. 폰이 없어 데스크톱 크롬으로 보여드리지만 원리는 같습니다. 세탁이 끝나는 순간 실제 푸시가 뜨고, 누르면 바로 그 손님의 세탁기 화면으로 이동합니다."

*(전환: "이걸 어떻게 만들었는지, 다시 정리해서 말씀드리겠습니다" → PPT)*

---

## 3막 · PPT — 기술·문제해결·Agent (2:55–4:35)

### 슬라이드 3 (기술적 특징) · 2:55–3:30
**제목:** 기술적 특징
- 실측 기반 전력 임계값 — 시작 100W / 탈수 500W / 종료 20W·60초 유지
- 단일 입구 `POST /ingest` — 개발 시뮬레이터 = 운영 센서, 같은 경로
- 6상태 상태머신 — IDLE → RUNNING → SPIN → DONE → COLLECTED / ABANDONED
- 종료 예상 시간을 서버가 계산 — 고객·사장님이 같은 값을 봄
- Web Push 딥링크 + 같은 알림 1회만(중복 발송 방지)

**나레이션:** "핵심은 다섯 가지입니다. 전력 임계값은 전부 실측값 — 시작 100, 탈수 500, 종료 20와트에 60초 유지. 개발 시뮬레이터와 실제 센서가 완전히 같은 입구 하나를 써서, 개발에서만 되고 매장에선 안 되는 사고를 막습니다. 종료 예상 시간은 서버가 계산해 고객과 사장님이 같은 값을 봅니다. 알림엔 이동할 링크를 실어 보내고, 같은 알림은 한 번만 나가게 막습니다."

### 슬라이드 4 (주요 문제 해결) · 3:30–4:05
**제목:** 주요 문제 해결
- **① 조기 '곧 끝나요' 알림** — 표준 코스는 탈수가 4번, 첫 탈수는 코스 초반 → 상태 대신 **시간 계산 타이머**로 교체
- **② 종료 오판 위험** — 히터 없어 세탁 중 전력이 뚝뚝 떨어짐 → 1초 CSV 실측(최대 8초) → **60초 hold**로 확정
- **③ 플러그 라이브러리** — 암호화 방식(TAPO) 미지원 → `tapo` 라이브러리로 교체

**나레이션:** "① '곧 끝나요' 알림을 첫 탈수에 걸었더니, 표준 코스는 탈수가 네 번이라 첫 번째는 코스 초반이었습니다 — 거짓 알림이었죠. 시간 계산 기반 타이머로 바꿨습니다. ② 60초 판정도 처음엔 근거가 없어, 1초 간격 CSV로 전력이 떨어지는 최대 시간 8초를 실측하고 그 일곱 배로 정했습니다. ③ 스마트플러그 라이브러리도 암호화 방식이 안 맞아 한 번 교체했습니다."

### 슬라이드 5 (Agent 활용 — RSAK) · 4:05–4:25
**제목:** AI Agent 활용 — RSAK
- **Rules** — 규칙을 `CLAUDE.md` 한 파일에 (단일 진실 원천)
- **Agent** — 역할별 서브에이전트 4종: 스펙분석 · 화면 · 서버 · 검증
- **Commands** — `/오늘`(브리핑) · `/팀` · `/pr`
- 원칙 — 스펙에 없는 결정은 임의로 정하지 않는다

**나레이션:** "규칙을 CLAUDE.md 한 파일에 몰아두고, 역할별 AI 에이전트가 그것만 참조하게 했습니다. 스펙 분석·화면·서버·검증을 나눠 맡기고, 매일 아침 백로그 브리핑 명령으로 할 일을 잡았습니다."

### 슬라이드 6 (클로징) · 4:25–4:35
**제목:** 빨래집사 🧺
- 방치를 없애는 게 아니라, 사장님의 운영 부담을 줄이는 것
- 감사합니다

**나레이션:** "빨래집사의 목표는 방치를 없애는 게 아니라, 사장님의 운영 부담을 줄이는 것입니다. 감사합니다."

---

## 촬영 전 준비물 체크리스트

**현장(실매장, 세탁기 4 라이브)**
- [ ] `simulate.mjs --list`로 세탁기 4 uuid 확보 (폴러는 uuid만 받음)
- [ ] `.env` VAPID·SUPABASE 키 확인, client `VITE_VAPID_PUBLIC` 확인
- [ ] Tapo P110M IP · H100 IP · T110 별명 현장 재확인
- [ ] 대상 세탁기 IDLE 확인 후 시작
- [ ] 노트북 화면녹화 + 세탁기용 카메라 2계통, 매장 Wi-Fi, 사장님 촬영 양해

**로컬 사전녹화(세탁기 3, 집에서 미리)**
- [ ] 60초 hold 클립 (`--scenario normal`)
- [ ] no-qr 방치 클립 (`TEST_ABANDONED_AFTER_MIN=1`)
- [ ] 크롬 실 푸시 → 딥링크 클립 (권한 dry-run 1회)
- [ ] (있으면) 안드로이드 실기기 스크린샷 위치 확인

**제작·PR**
- [ ] PPT 6장 제작 (1막 2장 + 3막 4장)
- [ ] 나레이션 후시 녹음(조용한 공간)
- [ ] 편집: 라이브 발췌 + 사전녹화 점프컷 + PPT 캡처 결합
- [ ] `git checkout -b feature/demo-video` (dev 분기)
- [ ] 영상 PR 첨부(용량 크면 외부 링크, 접근 권한 확인)
- [ ] `docs/backlog.md` T-26 상태 갱신 (커밋·PR은 서원 직접)
