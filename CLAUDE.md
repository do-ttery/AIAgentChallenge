# CLAUDE.md — 무인빨래방 AI 점장 에이전트

이 문서는 개발·디자인 Agent가 작업 전에 반드시 읽는 프로젝트 맥락입니다.
여기 있는 규칙과 결과물이 다르면 코드가 아니라 이 문서를 기준으로 고칩니다.

## 1. 프로젝트 요약

- **한 줄**: 무인빨래방의 보이지 않는 운영 노동(방치 감시·대응)을 대신하는 AI 점장 에이전트
- **문제**: 사장님이 매장에 없는 동안에도 방치 세탁물 때문에 원격으로 계속 감시·대응해야 한다
- **핵심 기능 2개**: ① 기계 상태 자동 감지(스마트플러그 전력 패턴 + 도어 센서) ② 방치 자동 대응(완료 알림 → 단계적 넛지 → 사장님 보고)
- **에이전트 원칙**: 사람 입력 없이 감지 → 판단 → 행동. 사용자가 요청해야 반응하는 기능은 이 서비스의 방향이 아니다
- 상세: `docs/plan.md` / 작업 단위: `docs/checklist.md` / 화면 프로토타입: `docs/prototype.html`

## 2. 디자인 시스템

화면·컴포넌트를 만들거나 검토할 때는 **`laundry-design` 스킬을 반드시 적용**한다.
색 토큰·타이포·레이아웃·도메인 UI 규칙·알림 문구 톤의 **전체 규칙과 검증 체크리스트**는 스킬에 있다
(`.claude/skills/laundry-design/SKILL.md`).

아래는 항상 지키는 **핵심 불변 규칙**(요약)이다:

- 상태 색 고정: 세탁 중=`accent`(#6366f1) / 완료=`ok`(#059669) / 방치·확인=`warn`(#d97706) / 대기=회색. **빨강 금지**(고객 비난 인상 금지)
- **예측 시각은 점이 아니라 범위로** ("18:45~18:55")
- **자동 처리=ok색 / 사장님 개입 필요=warn색** 으로 구분
- 폰트 `Pretendard` 계열, 카드 라운드 12~16px, 그림자는 카드에만 약하게

> 새 색·간격 등을 정하거나 규칙을 바꾸면 **스킬 파일에 반영**해 다음 작업의 기준으로 남긴다.

## 3. 개발 환경·컨벤션

- **스택**: React (프론트, Vite) + Express (백엔드, Node.js)
- **라이브러리 (확정)** — 아래 외 추가는 사유와 함께 이 문서에 기록 후 도입

| 영역 | 선택 | 사유 |
|------|------|------|
| 라우팅 | `react-router-dom` | 고객 `/m/:machineId`, 사장님 `/owner` 경로 분리 |
| 스타일 | 순수 CSS + CSS 변수(§2 토큰) + CSS Modules | 토큰 기반 일관성. Tailwind 등 유틸 프레임워크는 안 씀 |
| HTTP | 네이티브 `fetch` | MVP엔 별도 클라이언트 불필요 |
| 상태관리 | React 내장(useState/Context) | 규모가 작아 Redux 등 불필요 |
| 웹 푸시 | Web Push API + Service Worker + `web-push`(VAPID) | 완료·넛지 알림의 핵심 |
| DB | `better-sqlite3` (파일 SQLite) | 세션·전력·알림 저장. 동기 API로 단순 |
| 스케줄러 | 인메모리 `setTimeout` 큐 (필요 시 `node-cron`) | 30분/1시간 단계적 넛지 타이머 |
| 실시간 | 대시보드 폴링(5초), 부하 시 SSE 전환 | MVP 단순성 우선 |
| 설정/기타 | `cors`, `dotenv` | CORS 허용, 환경변수 |

- **디렉토리 구조**:

```
/client        # React 앱
  /src
    /components
    /pages     # 고객(m/*), 사장님(owner/*) 페이지
    /styles
/server        # Express API
  /routes
  /services    # 상태머신, 알림 등 도메인 로직
/docs          # plan.md, checklist.md, prototype.html
```

- **커밋 규칙**: `타입: 요약` 형식 — `feat:`(기능) `fix:`(수정) `docs:`(문서) `style:`(디자인·CSS) `chore:`(설정). 예: `feat: QR 랜딩 페이지 알림 신청 버튼 구현`
- **작업 단위**: `docs/checklist.md`의 체크박스 1개 = 커밋 1개를 기본으로. 덩어리가 커지면 쪼갠다
- **브랜치·PR**: 작업은 포크의 `N071_문도경_v2` → 원본 `N071_문도경`으로 PR. PR 본문은 저장소 템플릿 사용

## 4. 기술 결정 (개발 전 확정)

에이전트가 "감지 → 판단 → 행동"하려면 아래를 미리 확정한다.

1. **센서 데이터 수집 경로**: mock 엔드포인트 `POST /ingest/:machineId`(전력 W, 5초 간격)로 통일.
   - 개발 중: 시뮬레이터가 이 엔드포인트로 가짜 데이터 주입
   - 2주차~: 스마트플러그 로거가 **동일 엔드포인트**로 전송 → 개발·운영 경로 일치
2. **개발용 센서 시뮬레이터** (`server/scripts/simulate.js`): 실제 전력 곡선(시작 고전력 → 세탁 중 변동 → 탈수 스파이크 → 종료 0W)을 흉내 내 상태 전이를 실기기 없이 테스트
3. **웹 푸시**: Web Push API + Service Worker + VAPID. 권한 거부 시 대체 = 진행 중 화면 폴링으로 상태 확인하도록 안내
4. **인증**: MVP는 단일 매장 가정. 사장님 화면은 `env`의 `OWNER_PASSCODE` 단순 게이트. 멀티매장·정식 auth는 범위 밖
5. **저장**: SQLite 단일 파일 `server/data/app.db`. 이번 주 프로토타입(순수 HTML/CSS) 단계에는 DB 없이 화면만 만든다
6. **환경변수**(`.env`): `VAPID_PUBLIC/PRIVATE`, `OWNER_PASSCODE`, `PORT`. 실제 값은 커밋하지 않는다

## 5. 상태머신 · 데이터 모델

### 5.1 세션 상태 전이

```
IDLE → RUNNING → SPIN(탈수) → DONE
DONE → COLLECTED        (도어 열림 감지)
DONE → ABANDONED        (종료 후 30분 미개방)
ABANDONED → NUDGE_1 → NUDGE_2 → RESOLVED
(모든 단계) → ESCALATED  (전력/센서 이상 등 에이전트가 처리 불가)
```

- 전이 트리거: **전력 패턴**(시작/탈수/종료), **도어 센서**(열림), **타이머**(30분→방치, +30분→2차 넛지)
- 각 전이 시 알림·보고 액션은 §2.5 톤 규칙을 따른다

### 5.2 데이터 모델 (SQLite 테이블)

| 테이블 | 주요 컬럼 | 용도 |
|--------|-----------|------|
| `machine` | id, name, status | 기계 목록·현재 상태 |
| `session` | id, machineId, startedAt, endedAt, state | 세탁 1회 = 세션, 상태머신 보유 |
| `reading` | id, machineId, ts, watt | 전력 로그 (감지 근거) |
| `notification` | id, sessionId, type(출발/완료/넛지1/넛지2/보고), sentAt | 발송 이력 |

## 6. Agent 작업 지침

- 화면·컴포넌트를 만들 때 이 문서의 §2를 먼저 적용하고, 여기 없는 결정이 필요하면 **임의로 정하지 말고 질문**할 것
- 코드는 §4 기술 결정·§5 상태머신을 기준으로 구현한다
- 결과물이 §2와 다르면 코드 수정이 아니라 "이 문서 기준으로 재작업"
- 새로 확정된 결정(라이브러리 추가, 규칙 변경)은 이 문서에 반영해 다음 작업의 맥락으로 남길 것
